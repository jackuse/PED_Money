{
  "name": "MyMoney",
  "version": "0.0.1",
  "description": "MyMoney server",
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "test": "node_modules/.bin/mocha -w",
    "db": "mongod --dbpath ./db",
    "start": "node server.js",
    "startalll": "screen -dmS mongo npm run db && npm start",
    "startallw": "start npm run db && npm start"
  }
}