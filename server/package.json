{
  "name": "MyMoney",
  "version": "0.0.1",
  "description": "Money like web app",
  "dependencies": {
    "body-parser": "*",
    "express": "*",
    "mongoose": "*",
    "jsonwebtoken": "0.4.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "node_modules/.bin/mocha -w",
    "db": "mongod --dbpath ./db",
    "start": "node server.js",
    "startalll": "screen -dmS mongo npm run db && npm start",
    "startallw": "start npm run db && npm start"
  }
}